# Project-Local Supercell Instantiation Packet v1 — Instantiated

Purpose: one-file operator handoff artifact used to create the `supercell_ops` project-local supercell in one pass.

---

## 1. Signal Intake

- `project_name`: `supercell_ops`
- `source_repo`: `drop0ne/drop0ne-atticus-supercell`
- `source_branch`: `main`
- `project_supercell_repo`: `drop0ne/drop0ne-atticus-supercell`
- `baseline_version`: `ATTICUS supercell main`
- `active_packet`: `PROJECT_INSTANTIATION_PACKET_v1`
- `current_known_state`: `Global ATTICUS supercell is active, template pack is complete, and project-local instantiation workflows now exist in checklist, manifest, and one-file packet forms.`
- `active_next_step_focus`: `Preserve supercell architecture, instantiation process, and umbrella memory operations as a durable project-local surface.`

---

## 2. Authority Model

- Source repo is authoritative for:
  - umbrella supercell structure
  - template packs
  - global cross-model memory surfaces
- Project-local supercell is authoritative for:
  - supercell-ops long-term memory
  - normalized current ops state
  - supercell-ops resume packets
- Global ATTICUS supercell remains authoritative for:
  - umbrella cross-project continuity

Core invariants:
- one project-local authority root
- compact context-bearing memory cells only
- mirrors summarize source canon and link back to it
- resume packets must be reproducible from current state and durable memory

---

## 3. Required Files

Instantiated at:

```text
PROJECTS/supercell_ops/
├── README.md
├── CURRENT_STATE/
│   └── supercell_ops_state.md
├── MEMORY/
│   └── cells/
│       └── SUPERCELL_OPS_PROJECT_BASELINE_v1.json
└── SESSION_PACKETS/
    └── generated/
        └── supercell_ops_resume_2026-04-17.md
```

---

## 4. Current State Template

This instantiation uses the normalized current-state format for restart continuity and durable project-local state.

---

## 5. Baseline Memory Cell Template

This instantiation uses a compact context-bearing baseline cell only. No raw full-chat dump is used by default.

---

## 6. Resume Packet Template

This instantiation uses a compact project-local resume packet linked to durable baseline state.

---

## 7. Creation Checklist

### Phase 1 — Identity and authority
- [x] project_name set
- [x] source_repo set
- [x] source_branch set
- [x] project_supercell_repo set
- [x] authority boundary confirmed

### Phase 2 — Core files
- [x] README created
- [x] normalized current-state file created
- [x] baseline memory cell created
- [x] project-local resume packet created

### Phase 3 — Baseline normalization
- [x] active branch recorded
- [x] baseline version recorded
- [x] current known state recorded
- [x] active next-step focus recorded

### Phase 4 — Durable memory discipline
- [x] compact context-bearing cells only
- [x] provenance, scope, tags, lineage, and fingerprint included
- [x] no raw full-chat dump by default

### Phase 5 — Resume readiness
- [x] resume packet generated
- [x] packet linked to durable cells
- [x] recommended next action made explicit

---

## 8. Exit Criteria

- [x] project can be resumed from the project-local supercell without full original chat replay
- [x] authority boundaries are explicit
- [x] project-local supercell remains compatible with the global ATTICUS model

---

## 9. Operator Use Rule

This file served as the single operator handoff artifact for creating the `supercell_ops` project-local supercell in one pass.
